version: '3.8'

services:
  java-app:
    image: java-app
    container_name: java-app
    ports:
      - "8080:8080"
    networks:
      - my-network
    
    

networks:
  my-network:
    external: true
